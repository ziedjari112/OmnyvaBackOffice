export const environment = {
  production: false,
  apiUrl: 'https://localhost:44355/api',
  // Uploaded images (Products/Families) are served from the API's own origin at /uploads/**, not under /api.
  filesBaseUrl: 'https://localhost:44355'
};
